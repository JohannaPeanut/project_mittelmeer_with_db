"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _prngProxy = _interopRequireDefault(require("../../../prng/prngProxy"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

var RandomUuidGenerator = /*#__PURE__*/function () {
  function RandomUuidGenerator() {
    _classCallCheck(this, RandomUuidGenerator);
  }

  _createClass(RandomUuidGenerator, [{
    key: "generateRandom",
    value: function generateRandom() {
      return this._getUuid(_prngProxy["default"].random(23));
    }
  }, {
    key: "generateNext",
    value: function generateNext() {
      var randoms = [];

      for (var i = 0; i < 23; i += 1) {
        randoms[i] = _prngProxy["default"].next();
      }

      return this._getUuid(randoms);
    }
    /**
     * randoms should be length of 23
     * @param {RandomArray} randoms
     * @returns {string}
     * @private
     */

  }, {
    key: "_getUuid",
    value: function _getUuid(randoms) {
      var res = this._createUnixTimestampString(); // ********-xxxx


      res += Math.floor(randoms[0] * 0xF).toString(16) + Math.floor(randoms[1] * 0xF).toString(16) + Math.floor(randoms[2] * 0xF).toString(16) + Math.floor(randoms[3] * 0xF).toString(16) + '-'; // ********-****-4xxx

      res += '4' + Math.floor(randoms[4] * 0xF).toString(16) + Math.floor(randoms[5] * 0xF).toString(16) + Math.floor(randoms[6] * 0xF).toString(16) + '-'; // ********-****-****-yxxx

      res += (Math.floor(randoms[7] * 0xF) & 0x3 | 0x8).toString(16) + Math.floor(randoms[8] * 0xF).toString(16) + Math.floor(randoms[9] * 0xF).toString(16) + Math.floor(randoms[10] * 0xF).toString(16) + '-'; // ********-****-****-****-xxxxxxxxxxxx

      res += Math.floor(randoms[11] * 0xF).toString(16) + Math.floor(randoms[12] * 0xF).toString(16) + Math.floor(randoms[13] * 0xF).toString(16) + Math.floor(randoms[14] * 0xF).toString(16) + Math.floor(randoms[15] * 0xF).toString(16) + Math.floor(randoms[16] * 0xF).toString(16) + Math.floor(randoms[17] * 0xF).toString(16) + Math.floor(randoms[18] * 0xF).toString(16) + Math.floor(randoms[19] * 0xF).toString(16) + Math.floor(randoms[20] * 0xF).toString(16) + Math.floor(randoms[21] * 0xF).toString(16) + Math.floor(randoms[22] * 0xF).toString(16);
      return res;
    }
    /**
     * Generates random time in millis, then converts it <32 low bits>-<16 high bits> string
     * @private
     */

  }, {
    key: "_createUnixTimestampString",
    value: function _createUnixTimestampString() {
      var ts = Date.now();
      return (ts % 0xFFFFFFFF).toString(16) + '-';
    }
  }]);

  return RandomUuidGenerator;
}();

var _default = RandomUuidGenerator;
exports["default"] = _default;